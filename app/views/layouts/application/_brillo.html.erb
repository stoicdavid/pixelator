<div class="grid-container">
    <h6>Filtro Brillo (0 a 255)</h6>
	  	<% if @picture.image.attached?%>
			<%= form_with model: @picture do |form| %>
					<div class='grid-x grid-margin-x'>
				<%= form.fields_for :variation do |f| %>

							<div class='cell auto align-middle'>
								<%# f.range_field :bright_param, :in => 0..255, :step => 1 %>
								<%# slider(position,max,step,reference) %>
								<%= slider(50,255,1,'sliderOutput2','') %>
								<%= f.number_field :bright_param, id:'sliderOutput2' %>
							</div>
	
							<div class='cell auto grid-x grid-padding-x align-middle'>
								<%= f.submit 'Aplica Brillo', value: 'Brillo', class: 'hollow button ' %>
							</div>									
				<% end %>

					</div>
			<% end %>

		<% end %>
</div>